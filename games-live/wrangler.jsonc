{
  // GATED — separate Worker, NOT the Pages site. Do not deploy without Joel's OK.
  // Review against `durable-objects` + `workers-best-practices` skills first.
  "name": "neft-live-rooms",
  "main": "worker.js",
  "compatibility_date": "2026-06-01",
  "durable_objects": {
    "bindings": [{ "name": "GAME_ROOM", "class_name": "GameRoom" }]
  },
  "migrations": [{ "tag": "v1", "new_sqlite_classes": ["GameRoom"] }]
}
