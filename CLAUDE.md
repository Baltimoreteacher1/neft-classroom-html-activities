# CLAUDE.md — Repo Instructions for Claude Code

This is `neft-classroom-html-activities`, a mostly static HTML collection for Mr.
Neft's ESOL, Reading, Writing, and Math classroom activity hub. Most activities
are standalone HTML folders; the Reveal Math lesson launchers are built with Vite
so Cloudflare Pages can publish the complete `dist/` output.

For repo conventions (folder layout, naming, static-site rules, deployment
preset), also read [`AGENTS.md`](AGENTS.md). This file adds a mandatory quality
workflow that applies to **every** task.

---

## Closed-Loop QA Protocol

**Non-negotiable: do not hand back unfinished or unverified work.** On every task
in this repo, close your own feedback loop before claiming the work is complete.
Follow these stages in order.

### A. Understand

- Restate the actual goal in 1–3 lines.
- Identify the files, routes, components, scripts, or systems likely affected.
- Identify the likely failure risks **before** editing.

### B. Plan

- Create a short implementation plan before modifying files.
- Prefer minimal, targeted changes over broad rewrites.
- Preserve existing behavior unless the task explicitly requires changing it.

### C. Implement

- Make the required changes.
- Keep edits clean, readable, and maintainable.
- Avoid unrelated formatting changes or refactors.

### D. Verify

- Run the strongest relevant checks available in this repo (see
  **Verification Commands** below).
- Choose from: build, validate, audit, preview/smoke test, manual
  browser/route checks, link checks, Save/Resume integrity checks, or any
  project-specific audit script that touches what you changed.
- If no formal check covers your change, create a reasonable manual
  verification checklist and actually run through it.

### E. Failure Loop

- If any check fails, **do not stop.**
- Summarize the failure clearly.
- Fix the issue.
- Re-run the relevant check.
- Repeat until the check passes — or until genuinely blocked by a real
  external issue (e.g., missing secret, offline service), which you must
  name explicitly.

### F. Proof Before Handoff

- Before saying "done," provide:
  - **files changed**
  - **commands / checks run**
  - **pass / fail results**
  - **any remaining risks or skipped checks**
- Never claim success without verification evidence.

---

## Repo-Specific Verification Rules

For this repo, especially verify:

- **HTML activities still load correctly** — open or smoke-test changed
  `index.html` activity pages; no blank screens or console-fatal errors.
- **Shared assets/scripts are not broken** — the shared stylesheet path
  `/assets/shared.css` and shared scripts (`/assets/app.js`, vendor files,
  injectors) still resolve and load.
- **Save/Resume behavior is not accidentally removed** — see
  [`SAVE_RESUME_SYSTEM.md`](SAVE_RESUME_SYSTEM.md); run the Save/Resume audit
  when touching activities (`node tools/audit-save-resume-integration.js`).
- **Navigation links and lesson hubs still work** — root dashboard
  (`index.html`), unit/collection hubs, and internal links resolve to existing
  folders/files.
- **Cloudflare/Pages deployment assumptions are not changed without
  permission** — do not alter `_headers`, `_redirects`, `wrangler.toml`,
  `vite.config.js` output settings, `404.html`, or the deploy workflow unless
  the task explicitly asks. Preset stays: framework `None` / Vite build,
  output `dist/` (or `/` for pure static), root directory blank.
- **Student-facing activities remain usable without teacher PINs** unless the
  task explicitly requests gating.
- **No hidden teacher keys, answers, or dashboards are exposed to students**
  unless intentionally part of the task.

---

## Verification Commands

Use only commands that actually exist in `package.json` / repo tooling. Prefer
the strongest one(s) relevant to what you changed:

| Command                                       | What it does                                                                                                                                                                                                         | Use when                                                                                        |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `npm run validate`                            | Runs `validate:static` + `validate:reveal-math` + `validate:hub` + `validate:curriculum-top1` + `validate:save-resume`.                                                                                              | Any HTML, link, or structure change. **Primary check.**                                         |
| `npm run validate:static`                     | Validates the static site structure/links only.                                                                                                                                                                      | Static HTML/link-only edits.                                                                    |
| `npm run validate:js-syntax`                  | Parses every shipped `.js`/`.mjs` and every inline `<script>` block (~1,000 files + ~3,100 blocks) and fails on any SyntaxError. Part of `validate`. Added after `assets/game-fx.js` shipped to production truncated mid-function: the file is one IIFE, so nothing in it ran and the FX kit was dead across ~114 games, yet only `validate:lesson-boot`'s 16-page probe happened to notice. Syntax-only by design — Biome covers style. | Any JS edit, and any bulk/injector change that rewrites script files.                            |
| `npm run validate:lesson-boot`                | Boots 16 representative pages in a real browser and asserts each one actually RENDERED. **The only member of the gate that opens a browser — so if it cannot get one, it checks nothing.** Without the pinned Chromium it now SKIPS locally (exit 0, warns) and FAILS in CI (`CI` set), because it used to exit 0 either way and print `PASS validate:lesson-boot 1.0s`, indistinguishable from 16 pages rendering. Set **`PW_CHROMIUM_PATH`** to a system Chromium when Playwright's own download is missing or version-mismatched — a real run takes ~200s, not 1s, and that time difference is the tell. It also asserts `#nt-shell-fallback` is absent: the shell guard's "This lesson is having trouble loading" card is ~1072 chars, which sailed over the old 800-char content floor, so 10 lessons showed students an error card while this probe reported 16/16. | Any engine, renderer or lesson-shell change; always with a browser available. |
| `npm run validate:workflow-yaml`              | Rejects duplicate keys in `.github/workflows/*.yml`, then self-tests the scanner (10 cases) so a gate that stops firing fails loudly. GitHub REFUSES to run a workflow with a duplicate key — the run is created, dies instantly, and is labelled by file path instead of workflow name, which reads like an ordinary red check. `codex-verify.yml` sat dead for 16 days that way after a second `concurrency:` block landed in aa0a0aad0. Ordinary YAML parsers cannot catch it: PyYAML and js-yaml both accept duplicates and keep the last one. Part of `validate`, and runs first (it is instant). | Any `.github/workflows/*.yml` edit.                                                              |
| `npm run validate:reveal-math`                | Validates the Reveal Math tool launchers.                                                                                                                                                                            | Reveal Math / lesson launcher edits.                                                            |
| `npm run validate:save-resume`                | Audits Save/Resume wiring across every active activity (0 missing/duplicate/broken/unsentineled required). Part of `validate`.                                                                                       | Any change near activity state, Save, or Resume — enforced on every `validate` run.             |
| `npm run validate:ai-hub`                     | Regression guard for `curriculum/ai-hub/index.html`: inline script parses, every inline `on*` handler resolves to a defined function, the critical tutor-chat functions (`showTypingIndicator`, `startTutorChat`, `sendChatAnswer`, …) exist and are called, and no duplicate static ids. Part of `validate`. Added because concurrent rewrites twice shipped a dead tutor chat that the presence-only Playwright suite missed. | Any change to `curriculum/ai-hub/index.html`.                                                   |
| `npm run validate:injection`                  | Structural integrity for ALL sentinel injection layers (nsr/mobile-access/mwb/gfx/ghl/support-enhance/etc.): every `<family>-injected:begin/end` must balance per file. Auto-discovers families. Part of `validate`. | Any change to a `tools/inject-*` layer or bulk edit of injected pages.                          |
| `npm run validate:ccss`                       | Asserts every `standard` used by `lessons/*/config.json` exists in `data/ccss-standards.json` (the standards SoT, keyed by the 2025 MCCRS codes). Part of `validate`.                                                | Any change to lesson `standard` fields, `data/ccss-standards.json`, or a standards re-code.     |
| `npm run validate:reveal-assets`              | Asserts every file in `lessons/*/reveal-assets/` is referenced by tracked source, and every reference resolves on disk. These images have no generator owning their lifecycle, so both directions of the desync have shipped: replacing the Notice-and-Wonder stock photos with per-lesson SVGs re-pointed the configs and left 41 superseded rasters (6.58 MB) being copied into `dist/` and served to students for nothing. It builds the reference set from **all** tracked text files, never from the owning lesson's config — the 148 generated group/catch-up lessons cite the CORE lesson's assets (`2-7-group2` → `/lessons/2-7/reveal-assets/notice-wonder.png`), so a per-lesson check reports live classroom images as garbage, and its self-test pins exactly that case. **Retention is allowed but must be recorded**: an unreferenced file listed in `data/reveal-assets-retained.json` with a status and a written reason is reported, not failed — deleting the image a lesson used to show is an authoring decision, not one a QA gate makes alone. Exact paths only; the self-test proves a glob or a directory is rejected, because a broad ignore would re-open the very desync this gate exists to catch. The manifest is held to the same standard as the tree: an entry that vanishes from disk, or that comes back into use, FAILS. **This gate was documented as "Part of `validate`" for eight days while not wired into it at all** — the tool and this row landed on `main`, the `package.json` hunk lost a cherry-pick conflict on the one enormous `validate` line, and nothing noticed until it was run by hand. `tools/documented-gates-wired.test.mjs` now fails if this table names a script that does not exist, or claims membership of `validate` that `validate` does not honour. Part of `validate`. | Any change to a lesson's reveal images, to a `config.json` image reference, or to the retention manifest. |
| `npm run eval:small-groups`                   | **Fleet evaluation of the 148 generated small-group/catch-up lessons.** Treats the GENERATOR as the artifact under test: sweeps every config, reports per-stratum coverage to `reports/small-group-fleet-eval.md`, and fails on the defect classes no per-file gate can see — scaffolding that coaches the wrong operation (a `×` stem told to "line up the decimal points"), hints that state the answer outright, `errorStep` out of range, and choice feedback misaligned with choices. Self-tests all six detectors before sweeping, since a gate that stops firing reports a clean fleet. Found 89 real defects on first run. Part of `validate`. | Any change to a small-group generator, `tools/lib/small-group-*`, or bulk lesson-content edits. |
| `npm run validate:math`                       | **Checks that curriculum answers are arithmetically correct**, not merely present — exact rational arithmetic over every lesson `config.json` (1,819 decidable checks). Runs its own 51-case self-test first, so a gate that stops firing fails loudly instead of reporting a clean curriculum. Undecidable shapes are skipped, never failed. Part of `validate`. | Any edit to lesson answers, generators that emit answers, or `scripts/lib/rational.mjs`. |
| `npm run validate:surface-numbers`            | Asserts every number the canonical worked example (`launch.conceptIntro.iDo`) states also appears on the three surfaces that render it — `learn.html`, `slides.html`, `printable.html`. This is the complement to `generated-pages-fresh`, which proves a page matches what its generator would write and therefore cannot see a generator that faithfully writes the WRONG thing. Compares by VALUE, so 1,344 = 1344, 78.50 = 78.5, 50% = 50, 1/2 = 0.5 and "8 in." = 8 all pass; a page printing 1334 fails. 252 comparisons, measured at ZERO false positives — the alignment audit tried three other semantic comparisons (text anchors, cross-surface answers, vocabulary terms) at 100%, ~60% and 100% false-positive rates and none of them are here. Self-tests 9 cases first. Part of `validate`. | Any change to a worked example, or to `generate-notes`/`generate-slides`/`generate-printable-lesson`. |
| `npm run validate:concept-intro`              | Asserts the Build-the-Idea worked example (`launch.conceptIntro.iDo`) stands on its own: it references no artifact the card cannot render ("the picture" — the Build card renders a title and lines, never an image), and it never asserts an area/volume unit (`158 in²`) that no given carries. Both are string facts. It deliberately does NOT check whether an example states its givens: that heuristic (numbers in the conclusion with no antecedent) flagged 18 lessons and was wrong on almost all of them, because it cannot tell an unexplained answer from an estimation check or a verification substitution. Deciding which worked examples are weak is a reading task. Self-tests 10 cases first, including two regex traps that would make the unit check silently unsatisfiable. Part of `validate`. | Any edit to `launch.conceptIntro.iDo`. |
| `npm run validate:learn-figures`             | **Proves every Learn It worked-example diagram is read from its own lesson.** `scripts/lib/learn-figures.mjs` draws the parallelogram, box, number line, grid or bar model for the problem the worked example actually solves, by strict pattern-matching over `launch.conceptIntro.iDo` (25 of 74 lessons; it draws nothing when unsure). A picture that disagrees with the paragraph is worse than no picture — a student trusts the picture — so this gate asserts every measurement the figure claims appears in the lesson text, that no measurement label prints a number the reader never saw (axis ticks are exempt: they are scale, not claims about the problem), that the SVG is balanced and carries an aria-label, and that the figure reached `learn.html`. Self-tests 11 positive and 3 negative fixtures BEFORE sweeping, and fails if the sweep finds ZERO figures, so a reader that quietly stops matching cannot report a clean curriculum. Part of `validate`. | Any change to `scripts/lib/learn-figures.mjs`, or to a lesson's `conceptIntro.iDo` wording. |
| `npm run validate:lesson-visuals`             | Boots every lesson in a real browser, walks all 8 phases via `rma:navigate`, and asserts each interactive visual actually RENDERED. Source gates are blind here: `mountInteractiveVisuals` stamps `data-iv-mounted` *before* running the component factory and swallows a throw with a `console.warn`, so a dead manipulative still parses, still lints, and still serves 200 — and an unknown `kind` (REGISTRY miss) renders nothing with no warning at all. Fails loudly if it finds ZERO hosts, since a probe that navigates nowhere otherwise reports a perfectly clean site. Needs a preview server: `npm run preview -- --port 4499`. Weekly in CI via Site Health, not on every push (~220 lessons × 8 phases). Its FIRST half is pure source analysis and needs no browser — `--static-only` runs just that, and `tools/lesson-visuals-static.test.mjs` runs it on every `npm test`, so the "registered but unrenderable" class (net-folder sat in the REGISTRY with no `buildVisual()` case, rendering a blank gap in every full lesson that authored it) can no longer wait a week to surface. | After engine/renderer changes, new visual `kind`s, or anything touching `interactive-visual.js`. |
| `npm run smoke:live`                          | Post-deploy check against **production**: critical pages return 200 and look like themselves, gated surfaces still 401, shared bundles actually parse (the `game-fx.js` truncation class), and the build stamp reports the expected commit. `-- --expect <sha>` asserts the deployed commit. With `--expect`, a stale stamp is re-polled for up to 120s before failing: CF promotes across edge nodes over seconds, so a stale read right after `poll_stamp` is propagation lag, not a failed build — the two are told apart by whether it converges. A build that truly did not promote still fails, just 120s later. Run automatically at the end of `ship.sh` and weekly by Site Health. A Cloudflare Access interstitial is **NOT AVAILABLE**, not a Basic Auth outage. | After every deploy; when production "looks wrong" but source gates are green. |
| `npm run diagnose:student-access`             | Read-only classroom-network diagnostic. Harmless GET (no cookies written, `redirect: manual`) of homepage, curriculum, a student lesson, shared JS/CSS, and a public student API. Classifies each result as `PUBLIC / PAGES REACHED`, `CLOUDFLARE ACCESS INTERCEPT`, `APP AUTH INTERCEPT`, `UNEXPECTED RESPONSE`, or `NETWORK FAILURE`. A 200 whose body is the Access sign-in page is ACCESS, never PUBLIC. Exit 0 if student URLs reached Pages; exit 2 if Access intercepted them (run this from a Chromebook); exit 1 on unexpected/network. **Not a deploy gate** — it answers whether *this client* can see the student site. | From a classroom network, when Access vs open-site is in question. |
| `npm run diagnose:production-access`          | Read-only contract check for Canvas SCORM vs teacher protection. Probes the four representative SCORM families (lesson 1-1, lesson 5-1, homework 1-1, Ratio Color Mixer), configs/assets/student APIs, `www` vs apex, and teacher HTML/API. Student runtime must be `PUBLIC / PAGES REACHED`; teacher surfaces must not be public. Access intercept on a student URL exits 2 (Canvas SCORM would fail). A public teacher surface exits 3. See [`docs/cloudflare-access.md`](docs/cloudflare-access.md). **Not a deploy gate.** | After any Cloudflare Access change; before claiming Canvas SCORM works. |
| `npm run backup:d1`                           | Exports `neft-student-progress` (the only unrebuildable data here) and **replays the dump into SQLite to prove it restores** before accepting it. Writes to `~/neft-backups/d1/` — deliberately outside the repo, since backups hold student data and this repo auto-commits. Nightly in CI via `.github/workflows/backup-d1.yml`. | Before any D1 migration or destructive `wrangler d1 execute`; verify the nightly job is green. |
| `npm run report:usage`                        | Joins live D1 telemetry against the on-disk inventory: most-used lessons, games with recorded play, and lessons with no activity at all. Answers "what is actually used?" → `reports/usage-report.md`. | Before planning new content, and before retiring anything. |
| `npm run audit:dead-code`                     | One-pass reference graph over `assets/`, `scripts/`, `tools/`, `engine/components/`; reports unreferenced and single-referrer files with evidence. **Reports only — never deletes.** → `reports/dead-code.md`. | Debt burn-down; before adding another script that may duplicate one. |
| `npm run audit:small-group-ux`                | Walks every phase of a support and a challenge lesson at Chromebook and phone width and reports what a student would hit. **Reports only** — like `audit:a11y`, the findings need judgement, so it does not gate. It CRASHED with an uncaught Playwright `TimeoutError` until 2026-08-20: it located the name field as `input[type="text"]` and guarded with `.count()`, but `.count()` proves a node EXISTS, not that it can be typed into — the first text input on a small-group page is the hidden `.sg-room-code`, so `.fill()` blocked for its full 30s default and killed a report-only tool before it printed a finding. Now asks for `:visible`, bounds the wait, and exits 3 (SKIP) when no preview server is up instead of dying on the first locator. Needs `npm run preview -- --port 4499`; ~2.5 min. | Small-group shell, layout or phase changes. |
| `npm run audit:a11y`                          | axe-core (WCAG 2.1 A/AA) + keyboard-reachability pass over the highest-traffic student pages → `reports/a11y-audit.md`. Not a CI gate — a11y findings need judgement. | Accessibility work; after changing shared chrome, focus styles, or contrast tokens. |
| `npm run fix:save-resume`                     | Idempotent, reversible injector that adds the shared Save/Resume CSS+JS to any activity missing them (`--dry-run` / `--revert` supported).                                                                           | Remediation when `validate:save-resume` reports missing refs (e.g. after regenerating lessons). |
| `npm run validate:canvas`                     | Structurally validates built Canvas Common Cartridges in `canvas-packages/` (hrefs/module refs/tokens/links resolve). `build-library-cartridge` also self-validates before shipping.                                 | Canvas export / cartridge tooling changes (see `docs/canvas-bridge.md`).                        |
| `npm run validate:canvas-coverage`            | Asserts every assignable surface (catalog activities, injectOnly pages, lesson homework) exists on disk, carries the canvas-bridge sentinel, and has a unique SCORM package slug. Part of `validate`.                | Any change to `activity-catalog.json`, `inject-canvas-bridge.js`, or the SCORM builders.        |
| `npm run validate:downloads`                  | Gate for the bulk resource downloader. Asserts the GENERATED `data/curriculum-download-manifest.json` holds together: every resource belongs to a real unit, every `delivery:"file"` path exists and is non-empty on disk, no two entries share a ZIP path (a duplicate silently overwrites), presets select only the types they name, external/teacher-protected resources are represented as LINKS and never packaged, SCORM entries reuse the existing `/api/scorm`, every lesson in the curriculum manifest is covered, and both hub pages carry a current `?v=` content hash. Self-tests its detectors first. Part of `validate`. | Any change to the downloader, its generator, taxonomy, or the units page. |
| `npm run validate:unit-placement`             | **Which unit owns an End-of-Unit resource.** Order of authority (documented beside `CANONICAL_UNIT` in `scripts/lib/download-taxonomy.mjs`): 1. an explicit reviewed assignment; 2. the unit card in `curriculum/units/index.html` that contains the link, plus `data/curriculum-manifest.json` for lesson-scoped items; 3. the resource page's own `<h1>`/`<title>` against the CURRENT unit names — fails only on an unambiguous contradiction; 4. **the number in the path or filename is a diagnostic clue and may never fail a build.** Most unit-level assets kept their names through the 2026-08-10 Reveal-TOC renumber, so `/pre-test/unit9-review.html` is titled "Integers and Coordinate Plane" and belongs to Unit 7. An earlier pass trusted the path number, moved 29 correct placements and emptied Unit 10; `tools/unit-placement.test.mjs` pins the known cases so it cannot recur. `data/curriculum-unit-identities.json` is ALSO legacy-keyed — never use it to decide ownership. Part of `validate`. | Any edit to `curriculum/units/index.html`'s unit-resource rows, or when a resource looks like it is under the wrong unit. |
| `npm run validate:scorm`                      | Source-level: builds the SCO in-memory and asserts all **20** hardening invariants are present (cross-origin-safe API discovery, status read-before-write, LMS return-value and `LMSGetLastError` inspection, high-water score, `passed` never downgraded, suspend-data ceiling, `session_time`, Canvas identity, `<noscript>`), that the CLI builder still imports the shared `sco()` and does not shell out, and that `functions/api/scorm.js` probes the target (fail-open, 404-only). Every check is a **grep**: it proves a line exists, never that the runtime behaves — that is `scorm-lifecycle.test.mjs`'s job. `tools/scorm/template/` (a second hand-maintained SCO this gate used to keep in lockstep) was deleted, because a list of invariant strings can only pin the invariants someone remembered to add, and every hardening fix had to land twice. Part of `validate`. | Any change to the SCO wrapper, `/api/scorm`, or the CLI builders. |
| `npm run validate:scorm-runtime`             | **The SCORM Runtime v2 contract — the gate that spans the other SCORM checks.** `validate:scorm` proves the wrapper still contains its hardening LINES; `validate:scorm:fleet` proves all 554 packages OPEN; `validate:scorm-self-contained` proves they are live wrappers ON PURPOSE. None of them asks whether the four structurally distinct package families a teacher can actually download (lesson, homework, standalone activity, query-variant activity, plus save-codes and personalized variants) all come out of ONE runtime shell — which is the failure `tools/scorm/template/` used to be, two hand-maintained SCOs drifting apart so every hardening fix had to land twice. It also holds: version coherence across the three places a version lives (the wrapper, `assets/canvas-bridge.js`, `/api/scorm-probe`); that pre-flight actually REFUSES what it claims to (a missing manifest, a manifest launching a file not in the zip, a stripped runtime, a localhost or `.pages.dev` target, an embedded `TEACHER_KEY`, a lesson id the curriculum has never heard of); that download names are stable, unique, and free of every character Windows rejects; that Canvas titles are CANONICAL curriculum titles rather than raw slugs; and that two builds on the same day are byte-identical. **Nothing here is a grep for a line someone remembered to add** — every check builds a package and interrogates the result, and all eight negative cases run against deliberately broken inputs BEFORE the real ones, so a detector that has stopped firing fails loudly instead of reporting a healthy runtime. The BEHAVIOUR is proved separately by `tools/scorm/scorm-runtime.test.mjs` (19 scenarios) and `scorm-lifecycle.test.mjs` (21 checks), both in `npm test`, both driving the SHIPPED SCO in jsdom under `?ewlfast=1`. Part of `validate`. See [`docs/scorm-runtime.md`](docs/scorm-runtime.md). | Any change to `functions/_lib/scorm-sco.js`, `scorm-catalog.js`, `scorm.js`, `assets/canvas-bridge.js`, `/api/scorm-probe`, or a SCORM builder. |
| `npm run validate:scorm:fleet`                | **Opens every SCORM package the site can generate.** Builds all 554 (both delivery modes), parses each archive through a real ZIP reader, CRC-checks every entry, parses `imsmanifest.xml`, and asserts the launch file and every `<file href>` exist inside the zip, identifiers are valid NMTOKENs and unique fleet-wide, download names are filesystem-safe and unique, no entry path can escape extraction, no teacher-only material is packaged, and two builds are byte-identical. Self-tests all 12 detectors against known-bad fixtures first. Found the defect where every lesson's homework packaged to one identifier (`NEFT-homework-html`) and one filename, because the id came from the last path segment only — ~120 packages colliding, invisible to any per-package check. Part of `validate`. | Any change to `functions/_lib/scorm.js`, `assets/lib/zip-store.js`, the SCORM endpoints, or the activity catalog. |
| `npm run validate:scorm-self-contained`       | **Proves Canvas SCORM packages are live-site wrappers, not bundled lessons.** Builds representative packages (whole-group, interactive, homework, standalone activity), asserts each ZIP is only `imsmanifest.xml` + `index.html`, asserts every absolute URL in the SCO is the allowlisted EduWonderLab host, and — when a browser is available — launches the SCO with `eduwonderlab.com` **blocked** so a successful production fetch cannot masquerade as self-containment. Prints `VERDICT: PRODUCTION-DEPENDENT`. Not part of `validate` (the live-iframe design is intentional; this gate holds the contract). A missing browser SKIPs the runtime probe locally and FAILs in CI. | Any change to the SCO wrapper, `/api/scorm`, or before claiming Canvas SCORM works behind Cloudflare Access. |
| `npm run typecheck`                           | `tsc --noEmit` with `checkJs` over `assets/`, `engine/` and `shared/`. `npm run typecheck` prints the current coverage; the ratchet test below prints the split. Files not yet clean carry a `// @ts-nocheck` marker at the top — that marker IS the debt register, and removing one is the unit of work. The marker lives in the file rather than in a tsconfig list for a reason `exclude` cannot solve: tsc follows imports regardless of `exclude`, so one un-typed file re-contaminates every clean importer, and `engine/` is an entangled import graph. `types/globals.d.ts` declares the site's `window.*` handles, without which every window property is equally unknown and nothing about them is checkable. Part of `validate`. | Any change under `assets/`, `engine/` or `shared/`; before removing a `@ts-nocheck` marker. |
| `npm run audit:duplicates`                    | Groups tracked assets by content hash and annotates each copy with how many source files reference it. Duplication alone is not the signal — a vendored library loaded from two paths is fine — so it reports the bytes held by copies **nothing** points at. On first run it found 25.4 MB held in redundant copies, 12.6 MB of which nothing referenced at all (those were removed 2026-08-01; the rest all have live references and need a routing decision, not a delete). **Reports only — never deletes.** → `reports/duplicate-assets.md`. | Debt burn-down; before adding another copy of an asset that may already exist. |
| `npm run qa:fast`                             | **The inner-loop gate.** Runs only the checks that cover what you actually changed, in parallel — a lesson `config.json` edit costs ~1s instead of the full gate. It is DEFAULT-DENY: a changed path matching no rule in `scripts/qa-run.mjs`'s `COVERAGE` table escalates to the full gate, so it can never be *less* safe than running everything, only faster when coverage is provable. Not a substitute for the push gate — `qa:loop` still runs everything. | Constantly, while building. Cheap enough that there is no reason to skip it. |
| `npm run qa:loop`                             | **The full pre-push gate** (what the `pre-push` hook runs). **87 checks** (run `npm run qa:loop -- --list` rather than trusting this number — it had drifted from 48), scheduled by `scripts/qa-run.mjs`: `validate` is expanded into its members so they run concurrently, `build` is a barrier ahead of them because it rewrites the tree they read, and nothing runs twice. The old serial loop is still on disk as `npm run qa:loop:serial`. `tools/qa-run.test.mjs` (in `npm test`) reads the serial script's candidate list and fails if the parallel set ever stops covering it — a scheduler that gets fast by dropping a gate is worse than a slow one. **Wall time depends entirely on whether a browser is available: ~60s without one, ~240s with**, because `validate:lesson-boot` is the only member that opens one and it costs ~200s. That is not overhead to avoid — without a browser that check probes nothing (see its row below). | Before every push; automatically by the pre-push hook. |
| `npm run validate:lesson-supports`             | Gate for the **lesson adaptation layer** (`shared/supports/lesson-supports.js` — a teacher's support selection applied to the whole-group lesson and its small-group variants without touching either). Holds the invariant no lint, type check or render probe can see: **every support names a capability the engine actually implements** — a `PROFILE_KEYS`/`TOOL_KEYS` entry in `learning-supports.js`, or a taxonomy key wired to a `MODE_KEYS` mode in `supports-adaptations.js`. A support pointing at nothing renders a correctly-labelled toggle that changes nothing, and a teacher then records an accommodation as provided when it was not. It also pins: one impact class per support (`access`/`scaffold`/`modification`), that **no preset bundles a modification**, that supports attach to semantic element names rather than CSS selectors, that the stored profile is deltas only (no title, no URL, no student field), and that the manifest still carries `variants`/`intrinsic` — without which small-group inheritance and de-duplication both silently stop. It also holds the PRINT wiring (every print generator stamps the lesson id, loads the print layer and emits a semantic slot), that `print-supports.js` consumes the shared resolver rather than re-deriving anything, that every support declares a `MODALITY` for every surface, that every modification records its consequence in teacher language, and that `data/lesson-support-overrides.json` is not stale against the authored review — a suppression that exists only in the derived file has no reason and no evidence behind it. Self-tests its detectors first. Part of `validate`. | Any change under `shared/supports/`, `assets/learning-supports/`, `curriculum/student-supports/`, `teacher-tools/support-audit/`, a print generator, or the support review/override data. See [`docs/lesson-supports-architecture.md`](docs/lesson-supports-architecture.md). |
| `npm run validate:support-equivalence`         | **Screen, print and export must not disagree about what a teacher turned on.** Resolves every applicable support configuration on all 84 lessons through all three surfaces (1,588 configurations) and fails on any difference the `MODALITY` table does not declare. This is the only check that can see the failure it exists for: a teacher applies sentence frames, teaches from the supported lesson, prints the packet, and the packet has no frames — the screen code is correct, the print code is correct, and each is correct about a different thing. It also fails a support declared `active` on paper that contributes nothing to a printed page, which is the "toggle with no behaviour" lie one surface further along. Self-tests its detectors first. Part of `validate`. | Any change to `MODALITY`, `supportBlocks`, `print-supports.js`, or a print generator. |
| `npm run e2e:supports`                         | **The support workflow end to end, in a real browser**: supports surface → preset → preview → apply → whole-group lesson → small-group variant → printable → teacher notes copy → audit table → reset. 17 checks. Every individual surface has its own gate and none of them can see a workflow that breaks at a seam — inheritance working and the printed packet working still leaves "apply then print" untested. Needs a preview server (`npm run preview -- --port 4499`); `BASE=…` points it elsewhere. Safe by construction: everything it writes is localStorage on localhost. Not in `qa:loop` (it needs a server); run it before shipping a support change. | Any change to the support workflow, its surfaces, or the print layer. |
| `npm run validate:css-integrity`               | **Malformed style must not reach a browser, which recovers from it silently and tells nobody.** Three failure classes, all observed here: (1) a COMMITTED MERGE CONFLICT in `assets/small-group-designsystem.css` shipped to production — the browser parsed 131 rules where the file declared 143, and the twelve it dropped included a layout fix whose own comment described the defect it was written to fix; (2) a stylesheet losing a large share of its rules to a parse error, caught by counting what a real parser (jsdom, already installed — no new dependency) produces against the authored block count; (3) a `<style>` block living inside a JS template literal that a stray backtick **in a comment** truncated — the file still parses, so `validate:js-syntax` passes, the component throws at runtime where its caller swallows the error, and it renders unstyled. That one happened while writing Phase 4. It also greps the shared tool sheets for reintroduced decorative chrome (gradients, hover lift, infinite animation, sub-44px controls), and is explicit that a grep proves the source, not the rendered page. Conflict-marker detection is line-anchored so a Markdown setext heading and an ASCII comment rule are not false positives. Self-tests seven detectors; all five failure paths are mutation-proven. Part of `validate`. | Any change to a shipped `.css` file or a component that injects a `<style>` block. |
| `npm run sweep:small-group`                    | **The shared small-group shell, on all 204 generated variants, in a real browser at 1366x768.** The shell is styled centrally — three tokens and one stylesheet reach every variant — so a mistake reaches all 204 too, and three sample lessons cannot see it. Asserts the things a screenshot cannot: no horizontal overflow, the masthead stays under 520px (it ran 581-615px before the 2026-08-15 redesign, which put the first mathematical task at or below the fold on a classroom laptop), the page canvas has no background image, the shell carries no gradient wash or heavy shadow beyond the two elements that legitimately float (the annotation rail and the vocabulary dialog), and the page actually rendered content and phase tabs — a calm page that renders nothing is not an improvement. Measurements are scoped to `sg-` elements: shared interactive components (the long-division lab's transport controls) decorate themselves and are out of this pass's scope. Needs a preview server (`npm run preview -- --port 4499`). | Any change to `engine/core/small-group-ui.js` tokens, `assets/small-group-*.css`, or the small-group shell. |
| `npm run validate:planning`                   | Gate for the Live Pacing Planner (`/curriculum/planning/`). Beyond the scaffolded file/scope checks it holds the three things no per-file check can see: (1) the **day-type and actual-status vocabulary agrees across all three places it lives** — `shared/pacing/engine.js`, the API's allow-list in `functions/api/pacing/[[path]].js`, and the Edit Day form; a type added to one and not the others reads as a save that 400s or a day the form cannot describe. (2) `data/pacing-baseline-2026-27.json` still holds 210 dates / 180 instructional days, schedules only ids that exist in the launch manifest, and **stores no curriculum lesson title** — a stored title is how a plan keeps showing a name the curriculum has since renamed. (3) `/curriculum/planning` is still behind the teacher gate, and the pacing endpoint still references no student field. Part of `validate`. | Any change under `curriculum/planning/`, `shared/pacing/`, `functions/api/pacing/`, or to the pacing baseline. |
| `npm run validate:plan-notes`                  | Gate for `/curriculum/plan-notes/`. Asserts owned files exist, scripts parse, the generated `functions/_lib/plan-vocab.js` matches a fresh `npm run generate-plan-vocab` (a stale vocabulary silently rejects misconception tags the labels file already has), and notes cannot disappear when a quote stops matching. Part of `validate`. | Any change under `curriculum/plan-notes/`, `functions/_lib/plan-vocab.js`, or `scripts/generate-plan-vocab.mjs`. |
| `npm run generate-plan-vocab`                  | Rebuilds `functions/_lib/plan-vocab.js` from misconception labels, standards, catalog, and the curriculum manifest. Pages Functions cannot read `data/*.json` at runtime, so this compiled module is the API's vocabulary. `--check` is what `validate:plan-notes` runs. | After adding a misconception tag, a standard, a catalog entry, or a lesson. Also runs as part of `curriculum:rebuild`. |
| `npm run validate:curriculum-links`            | Asserts every `/lessons/<id>/` link inside a unit block on `curriculum/units/index.html` belongs to that unit per `data/curriculum-manifest.json`, and that every non-hidden manifest lesson appears on the hub. Complements `audit:links` (which cannot see a live link pointing at the wrong unit). Part of `validate`. | Any edit to the units hub or the curriculum manifest. |
| `npm run validate:lesson-catalogues`           | **A lesson catalogue must name the lesson it links to.** Three catalogues decide what `/curriculum/units/` offers on each lesson row — `LESSON_BONUS_ACTIVITIES`, `LESSON_PRINTABLES` (inline in `assets/curriculum-hub-search.js`) and `LESSON_FAMILY_HOMEWORK` — and the hub resolves them as `MAP[lessonId] \|\| MAP[baseLessonId]` using the id it reads off that row's own `/lessons/<id>/` link. Two of the three still carried the **pre-2026-08-10 Reveal-TOC numbers**. Both id spaces are `\d+-\d+`, so nearly every lookup HIT and returned a different lesson's material: measured in Chromium, 147 of 174 rows offering a bonus activity linked to another lesson, and 147 of 174 offering printables served another lesson's worksheet — lesson 1-1 "Math is Mine" handed out lesson 6-13's Prime Factorization worksheet. **Every one of those links resolved HTTP 200**, which is why no link checker, build, lint or render probe could see it. Three invariants, none needing an external crosswalk (a crosswalk is one more thing that goes stale): **key agreement** (a key whose own entries all link elsewhere is mislabelled), **disk in both directions** (key set == lessons with the material on disk, each surface reporting its own consequence), and **parents as well as children** (the hub renders 252 rows, not 84; every variant resolves through its parent, so a variant key silently diverges the twins and an orphan variant folder can only render empty). Self-tests six detectors against known-bad fixtures first. Part of `validate`. | Any edit to those three catalogues, to `curriculum-hub-search.js`, or to a generator that writes them. |
| `npm run validate:drive-sync`                  | Structural + behavioural gate for `scripts/sync-curriculum-to-drive.mjs`. Asserts the sync still reads generated manifests (no hardcoded lesson list), never deletes, and that `--verify` against a throwaway dest reports a leftover file without removing it. Does not touch the real Google Drive mount. Part of `validate`. | Any change to the Drive sync script. |
| `npm run validate:notebook`                    | **Every core lesson asks students to write in their paper notebook, two times, at the right moments.** Checkpoints are declared once in lesson data (`notebook.checkpoints`) and rendered by the engine — never hand-added to lesson HTML. Holds what nothing else can see: exactly two checkpoints per core lesson with boxes 1-2 (zero FAILS — a lesson that quietly opted out looks identical to one nobody reached; declaring a 3rd box FAILS); every checkpoint on a phase id the engine actually has AND a section that lesson actually ships (a checkpoint on a nonexistent phase renders nowhere and gates nothing while looking authored); one canonical Math Notes model at `/assets/math-notes/math-notes-model.svg` with no per-lesson copies; the default classroom copy existing in exactly ONE file, because copies drifting apart is this repo's recurring defect shape and this copy is a norm that must read identically in every lesson; and the `suspend_data` guard key still matching between the engine and `assets/canvas-bridge.js`. It PRINTS two coverage numbers on every run — lessons with authored prompts and lessons on defaults — because a silent 100%-default forever is not visible in a pass/fail. Self-tests detectors against known-bad fixtures before sweeping. Runs inside `npm run validate:hub`, which `validate` chains — so it gates every deploy without appearing twice in the chain. | Any change to a lesson's `notebook` block, `engine/core/notebook-checkpoint.js`, the model asset, or the canvas bridge guard. |
| `npm run validate:copy-panels`                | **Every word a student copies by hand must trace to the lesson it appears on.** `validate:notebook` checks a copy panel's SHAPE — 3-5 items, a rule line, no emoji, under 12 words — and it passed on all 84 lessons while 39 of the 84 box-2 rules stated another lesson's mathematics: lesson 2-6, "Divide Multi-Digit Numbers Using an Algorithm", told students to copy `Interquartile Range: IQR = Q3 − Q1`, which is lesson 2-5's content. The shape gate did worse than miss it — it **required** a `copyPanel` on every checkpoint, so a lesson with nothing quotable had to be given something, and the only available something was invention. Requiring presence is what manufactured the 84/84 "complete" report. Every check here is a substring comparison between the panel and another field of the SAME config: a box-1 term must be declared in that lesson's own `vocabulary`, its definition must be a literal run of that lesson's own definition (shortened, never rewritten), a box-2 rule and meaning must be stated in that lesson's `launch.conceptIntro.keyIdea`, and the example must be printed by that lesson's own worked example. The notebook block is never its own evidence. **Absence is a PASS** — a lesson with no quotable rule correctly gets no box-2 panel — and coverage is printed on every run, with a WARNING if all 84 carry both panels, because full coverage is the shape the invented content took. Four negative controls (one of them the exact string that shipped on 2-6) and three positive controls run BEFORE the sweep. Measured: 397 failures against the shipped content, 0 against the rebuild. Part of `validate`. | Any change to a lesson's `notebook.checkpoints[].copyPanel`, its `vocabulary`, or `launch.conceptIntro`. |
| `npm run generate:copy-panels`                | Rebuilds every copy panel from its own lesson: box 1 from that lesson's declared vocabulary with definitions shortened to verbatim prefixes at clause boundaries, box 2 quoted from `launch.conceptIntro.keyIdea` with an equation lifted from `iDo.lines`. It composes no prose. A lesson stating no quotable rule gets no box-2 panel and is **named in the output** — the mixed result (28 lessons with both panels, 56 with one) is the correct outcome, not a gap to fill. `--check` fails if any config is stale. | After editing a lesson's `vocabulary` or `conceptIntro`; never hand-edit a `copyPanel`. |
| `npm run sweep:math-notes`                   | **Opens the Math Notes dialog on all 84 core lessons and proves it shows that lesson's own words.** The dialog used to render only the blank page-layout model, so a student opening "Math Notes" saw a picture of a notebook and none of today's vocabulary — the lesson's actual words lived two phases away inside the checkpoint blocks, and nothing said so. It now renders the SAME verified panels the checkpoints render, and this sweep holds the wiring: the dialog must name THIS lesson, every term in it must be declared by THIS lesson's `vocabulary`, and any rule must be a run of THIS lesson's `keyIdea`. `validate:copy-panels` proves the DATA is lesson-own; this proves the right data reaches the dialog on **all 84** rather than the 4 the render gate samples. Needs a preview server (`npm run preview -- --port 4499`); `BASE=…` points it at production. Not in `qa:loop` (it needs a server). | Any change to `openMathNotesModel`, the checkpoint panels, or a lesson's vocabulary. |
| `npm run report:math-notes`                  | Per-lesson report of what Math Notes actually offers a student — Section 1 terms, Section 2 anchor, the field each string was quoted from, and whether the lesson has a concrete mathematical anchor → `docs/math-notes-coverage.md`. Report only; the gates are `validate:copy-panels` and `sweep:math-notes`. | After regenerating copy panels, or when asked what a lesson's notes contain. |
| `npm run validate:shared-claims`             | **Shared code may not teach one lesson's mathematics to every lesson.** Every other gate here asks whether LESSON DATA belongs to the lesson it appears on; a string hardcoded inside a shared component is not lesson data, so all of them are blind to it — and it shipped twice. `data-live.js` printed *"These are categories, so a mean or median has no meaning — that is what makes this a bar chart, not a histogram"* under **every** bar chart on every lesson, including 3-8 "Solve Problems with Unit Rates", which teaches none of that; and `assets/math-notes/math-notes-model.svg` drew a sample page headed "Lesson 1-1 · Sept. 3" showing `Area = base × height`, rendered in the Math Notes dialog of all 84 lessons. Two deliberately narrow detectors: a **cross-representation claim** (a contrastive sentence — "not a", "instead of", "is what makes", "has no meaning" — naming two or more distinct representations; explaining the thing you ARE drawing needs no contrast with the thing you are not) and a **fabricated lesson identity** (shared code printing a literal `Lesson 4-2` no lesson supplied). Narrow on purpose: flagging every sentence that mentions mathematics inside `engine/components` reports ~240 findings, nearly all correct — a box-plot builder SHOULD explain quartiles — and a gate needing a 240-line allowlist on day one is a formality. Both detectors are mutation-proven against the exact strings that shipped, and 5 positive controls pin the component copy that must stay quiet. Legitimate cases live in `data/shared-component-claims-review.json` with a written reason; reasons under 40 characters fail, and a decision whose finding no longer fires must be deleted. Part of `validate`. | Any change under `engine/components/`, `engine/core/`, `shared/`, or `assets/math-notes/`. |
| `npm run validate:secrets`                     | Static scan of tracked text for high-confidence credential shapes (PEM keys, GitHub PATs, AWS access keys, Slack tokens, OpenAI/Google API keys). Self-tests each detector before sweeping. Part of `validate`. | Any commit; especially anything that might have copied a `.env` value. |
| `npm run validate:worksheet-audience`          | Student `lessons/*/worksheet.html` must not inline answer keys (`ws-correct` markup, `Answer Key` headings, `ws-keynote`). Keys live on `worksheet-answer-key.html`, which `isTeacherSurface` already gates via the `answer-key` substring — this gate does not edit the frozen auth predicate. Self-tests first, including the CSS `.ws-correct{` trap. Part of `validate`. | Any change to `generate-worksheets.mjs` or `lessons/*/worksheet*.html`. |
| `npm run audit:deps`                           | Allowlisted `npm audit` (`tools/audit-allowlist.mjs`). CI (`predeploy-verify.yml`) already calls this; the script must exist. Not part of `validate` (needs network). | Dependency or allowlist changes; CI. |
| `npm run validate:production`                  | Production-readiness orchestrator: `validate` + honest `validate:lesson-boot` + read-only `smoke:live`. Distinguishes PASS / FAIL / SKIPPED / NOT AVAILABLE IN THIS ENVIRONMENT. A skipped required check (no browser, no network) is exit 2, not green. `--no-live` skips contacting production. Does not deploy. | Before calling a change production-ready; after `qa:loop` when you need skip honesty. |
| `npm run validate:interactive-alignment`      | **The right tool for the right mathematics.** Lesson 1-4 ("Math is Explaining and Sharing", 5.MD.C.5) shipped a trapezoid AREA explorer labelled "Garden footprint: the 6 ft bed and the 4 ft bed share a 2 ft width" in a lesson whose entire content is defending a cereal-box packaging choice with VOLUME — and whose own practice item offers "240 square inches" as a tagged distractor to correct exactly that square-versus-cubic confusion. **Every existing gate passed it**: the kind was in the REGISTRY, it had a tool-catalog entry, its required fields were present, it rendered, it was reachable from the tools drawer. They all ask "does this tool WORK?"; none asked "does it BELONG?". Root cause was not authoring — `tools/lesson-tool-coverage.test.mjs` requires a tool in Explore AND Practice fleet-wide, and a coverage quota with no alignment requirement does not produce missing tools, it produces irrelevant ones. Three decidable detectors, since software cannot judge pedagogy from a component name: **TOPIC** (the tool's mathematics against the standard's own `topic` field in `data/ccss-standards.json` — read, never inferred from the code shape; two earlier hand-rolled taxonomies flagged 150+ correct tools before this), **NUMBERS** (a tool whose every configured quantity is absent from its lesson is drawing a different problem), **CONTEXT** (a label naming a scenario the lesson never mentions is a label copied from elsewhere). Cosmetic and axis fields are exempt — `max: 22` is scale, not a claim. **The evidence must exclude the tool itself**: the first version compared each tool's numbers against a lesson set computed by walking the config that CONTAINS the tool, so every tool matched itself and the fleet looked perfect; `withoutInteractives()` and a self-test pin that. A flag is EVIDENCE, not a verdict — 2 of the 3 first-run findings were correct tools with unusual configurations. The ratchet is `data/interactive-alignment-review.json`: every flagged element needs a written decision (reasons under 40 chars fail, which is how an audit gets gamed), variants inherit only via an explicit `appliesToVariants`, and an entry whose flag no longer fires must be deleted so the file cannot become stale absolutions. The build prints **interactive mappings awaiting review**; the target is 0. Self-tests replay the shipped 1-4 config and fail if either detector stops catching it. Part of `validate`. | Any change to a lesson's interactive `kind` or its configuration, to `TOOL_TOPICS`, or to a generator that assigns tools. |
| `npm run validate:learn-it-scope`             | **Does a lesson's Learn It teach THAT lesson?** The companion to `validate:interactive-alignment`: that gate asks whether the right TOOL is mounted, this one asks whether the EXPLANATION stays inside the lesson it belongs to. Lesson 5-10 ("Volume of Rectangular Prisms", 6.GR.2 — volume with fractional edges, via base area × height) needed both. It mounted `manip:cube-builder` in TANK mode, whose steppers truncate to whole numbers (`L | 0`, so the lesson's own 1.5 ft edge is not expressible) and whose readout prints the OPEN-TOP SURFACE AREA of five glass faces — 6.GR.4, lesson 5-6's mathematics. It also carried a full vocabulary entry for **Net** (term, translation, definition, cloze, example, non-example, and a slot in the printed word search) for a concept it never asks a student to use. Meanwhile the method its own objective names, base area × height, appeared in the objective, the vocabulary, the Turn-and-Talk extension and the extending practice — everywhere except the worked example that teaches it. Three decidable detectors, each a string fact compared against another string fact about the SAME lesson: **ORPHAN** (a vocabulary term the lesson defines in full and never uses again — evidence taken with the vocabulary block REMOVED, since a term's own definition and cloze otherwise prove it in use and every term passes), **UNTAUGHT** (a term named in BOTH the `contentObjective` and the vocabulary that the Learn It never mentions — anchored to the intersection because the objective alone flags ordinary English), and **FOREIGN** (Learn It prose asserting a named formula from a different `topic` than the standard's own, read from `data/ccss-standards.json` exactly as `TOOL_TOPICS` is). Stems are truncated to six characters so ordinary inflection is not read as absence — the first sweep reported 1-2 as never using "quantity" while its objective says "compare quantities". Variants are NOT swept: they are generated from the parent and inherit its vocabulary, so auditing them repeats every parent finding three times and adds no fact. The ratchet is `data/learn-it-scope-review.json` — every finding needs a written decision, reasons under 40 chars fail, and a decision whose finding no longer fires must be deleted. Self-tests 10 fixtures (including the shipped 5-10 config) BEFORE sweeping. The build prints **Learn It scope awaiting review**; the target is 0. Part of `validate`. | Any edit to a lesson's `vocabulary`, `contentObjective`, or `launch.conceptIntro`. |
| `npm run validate:learn-slide-alignment`     | **The problem the teacher projects is the problem Learn It teaches.** The whole-group deck (`lessons/*/slides.html` + `slides.pptx`) and the in-lesson Learn It stepper both derive from ONE canonical field, `launch.conceptIntro` — the right design, but two drifts remain possible and this gate holds both. (1) The COMMITTED deck vs the config the panel fetches at runtime: it parses the deck's embedded `conceptData` (machine-readable JSON, no HTML scraping) and requires the iDo/weDo/youDo number multisets to match exactly (1,344 = 1344, ½ = 1/2, 78.50 = 78.5) AND the normalized text to be identical — text identity is what holds the conceptual/statistics lessons to the same standard instead of exempting them. This is deliberately independent of `generated-pages-fresh`, which trusts the CURRENT generator and therefore cannot see a generator edit that starts baking a different field into the worked-example slide. (2) Learn It's own presentation layer: every equation strip, keyIdea formula/example tile, and guided ask/tell split must draw its numbers from the very line it presents (multiset containment) — the model test proves extracted equations are arithmetically TRUE, but true-and-foreign is still the wrong problem. Its first sweep caught exactly that class live in production: `0.7v = 26,600` displayed as the false `7v = 26,600` (4-5) and `2.5h = 1,000` as `5h = 1,000` (9-4), both runs starting after the decimal point. Variants are not swept (generated from the parent, same rule as `validate:learn-it-scope`). Self-tests 6 detectors BEFORE sweeping, including the canonical 936 ÷ 4 vs 784 ÷ 7 substitution, so a detector that stops firing fails loudly. Part of `validate`. | Any edit to `launch.conceptIntro`, `scripts/generate-slides.mjs`, `engine/core/learn-step-model.js`, or the Learn It panel. |
| `npm run validate:generator-safety`           | **A generator must not damage what it did not mean to touch, and must not publish a route to a file that is not there.** Two observed failures. (1) AUTHORED LAYERS ERASED: small-group configs are GENERATED and are also the file two later steps write into — the Spanish overlay from `data/es-translations`, and `launch.conceptIntro.interactiveVisual`, a lesson's explicit statement of which tool it wants. The generator rebuilt each config from its base and wrote it whole, so both were erased; reproduced on `main`, where `--only 5-10` deleted 74 lines from `5-10-group1` taking ten Spanish arrays with it. The repo's answer had been a documented WORKAROUND (`--facilitation-only`, "a full run is destructive") plus one bespoke preserver for vocabulary — a workaround is a note asking humans to remember. The rule is now one sentence: **the generator owns what it emits, and anything on disk it does not emit is authored and survives** (`tools/lib/authored-overlay.mjs`, `--prune` to opt out). Array elements are paired by IDENTITY (`id`/`stem`/`term`/…) and never by index, because index-merging is worse than losing the data — it attaches one question's Spanish to a different question. (2) GENERATED ROUTES TO MISSING FILES: every button on an editable-slides page is the `.pptx`, and the generator emitted a page for all 84 lessons when 20 have no deck, publishing 20 launchers whose every button 404s; it now preflights the artifact and skips with a report. A third instance — `generate-catchup-lessons.mjs` — was found by this gate, not by a human. Also enforces write-set containment: `tools/lib/write-set.mjs` records every write and a scoped run FAILS if it wrote outside its scope, so `--only 5-10` is an assertion rather than a hope. Source-level by design (greps + file existence); the BEHAVIOUR is proved by `tools/generator-safety.test.mjs`, which drives the merge with the shapes that actually broke. Self-tests six detectors first. Part of `validate`. | Any change to a generator that writes lesson configs or generated launcher pages. |
| `npm run validate:auth-contract`              | **Authentication is FROZEN at `4c2e13dab` (tag `auth-known-good`); this gate is what keeps it there.** The model is documented in [`AUTH_CONTRACT.md`](AUTH_CONTRACT.md) — canonical-host 308 first, HTTP Basic with `SITE_PASSWORD` on teacher surfaces only (the site is student-open), `x-teacher-key`/`TEACHER_KEY` on `/api/*`, and a client-side Teacher Mode PIN that is deliberately none of those. It holds two kinds of fact. **Invariants**: the redirect is still a 308 and still runs before the gate, the realm is unchanged (browsers key saved passwords by realm, so renaming it silently forgets every teacher's password), an unset `SITE_PASSWORD` still 503s instead of serving teacher material, and neither cookie/session auth nor `Sec-Fetch-Mode` branching has returned. **A content pin**: sha256 of the five auth-critical files in `data/auth-baseline.json`, so ANY edit fails until re-pinned on purpose with `--update`. That is deliberately annoying — it converts "I touched `_middleware.js` while doing something else" from an invisible event into a blocking one. Why it exists: a teacher sign-in rewrite shipped 2026-08-16 at 08:04, grew four follow-up commits over eleven hours, and never signed a single teacher in, while every existing gate stayed green — each asked whether the code was well-formed, none asked whether the auth model was still the one that works. Every check here is a source-text fact; the BEHAVIOUR is proved by `tools/auth-contract.test.mjs` (14 assertions against the real module, in `npm test`) and by `npm run e2e:auth`. Self-tests 8 detectors against mutated fixtures first. Part of `validate`. | Any change to `functions/_middleware.js`, `functions/_lib/teacher-surface.js`, `engine/core/teacher-mode.js`, `curriculum/planning/planning-store.js`, or `functions/api/pacing/`. |
| `npm run e2e:auth`                            | **The teacher's actual path, in Chromium AND WebKit.** Starts its own `wrangler pages dev` with a THROWAWAY password, so the full flow really executes: site open anonymously → planner challenges with Basic → the password opens it → refresh still works → other teacher surfaces open → the pacing API refuses an anonymous write → a wrong password is refused. Then it **compares the two engines check by check and fails on any divergence** — the rewrite that caused the outage branched on `Sec-Fetch-Mode`, which Safari omits, so Chromium saw a login page and WebKit saw a Basic dialog checking a different secret; one-engine coverage cannot see that class of bug at all. `--production` runs the read-only half (the password half cannot run against production without Joel's real secret, and a run that stops at the 401 has verified the lock, not the key) and additionally proves www → apex canonicalization, which localhost cannot exercise. A missing browser is reported **SKIP and exits non-zero** unless `--allow-skip`. Needs `npm run build` first. Not in `qa:loop` (it starts a server); run it before shipping any auth change. | Any change to the files above; before any deploy that touches auth. |
| `npm run validate:pacing-unit-order`         | **The Unit dropdown follows district pacing; lessons inside it follow instructional order.** Two orderings that go wrong by doing nothing. The curriculum numbers its units 1..10 and the district teaches Pre, 3, 4, 6, 7, 8, 9, 5, 2, 10 — so reading the manifest in its own order, or any `.sort()`, produces a dropdown that ASSERTS the wrong sequence, and costs a teacher in November six units of scrolling to reach the one they are in. Lesson ids are strings, so a lexical sort puts 6-10 and 6-11 between 6-1 and 6-2. The order comes from `data/pacing-unit-ranges.json` (generated by `tools/import-pacing-baseline.mjs`; never hand-edit) and this gate holds: the plan is internally sound (contiguous sequence, no unit paced twice, dates non-overlapping); **reconciliation in both directions** — a paced unit the manifest lacks is a dropdown entry with no lessons, and a manifest unit the plan forgets is reported rather than silently appended; the resulting order is neither numeric nor alphabetical, stated as a PROPERTY so it survives a district re-plan; every unit's lessons ascend by numeric suffix; and `assets/curriculum-teacher-planning.js` still reads the plan and does not sort it. A pacing entry with no `curriculumUnit` (MSTAR) is not a Unit option — it owns no lessons and would render an empty Lesson dropdown. Seven mutation tests run BEFORE the real data, because a detector that has stopped firing reports a perfectly ordered curriculum. Part of `validate`. | Any change to the pacing baseline, `data/pacing-unit-ranges.json`, the hub picker, or lesson numbering. |
| `node tools/import-pacing-baseline.mjs`       | Re-seeds `data/pacing-baseline-2026-27.json` (the ORIGINAL plan), `data/pacing-unit-ranges.json`, and `assets/pacing-unit-dates.generated.js` from `docs/pacing-sources/plan-baseline.json`. **Run it only to re-seed** — live pacing lives in D1 behind `/api/pacing`, and a re-import does not touch it. `--check` fails if any generated file is stale. | After a source calendar or scope-and-sequence change; never during the school year without re-reading `docs/pacing-sources/planning-notes.md`. |
| `npm run new:surface -- <slug> --title "…"`   | Scaffolds a new `/curriculum` surface **already wired**: page + scoped stylesheet + module, a real `tools/validate-<slug>.mjs` gate linked into `npm run validate`, the `data/routes.json` redirect, the hub card, and a `qa:fast` coverage rule. Every edit is anchored and additive — no file is re-serialised (`data/routes.json` is not Biome-formatted, so a `JSON.stringify` round-trip buries a 5-line change in a 97-line diff). `--dry-run` prints the plan. | Adding any new surface under `/curriculum/`. |
| `npm run build`                               | Vite production build to `dist/`.                                                                                                                                                                                    | Anything touching Vite-built lesson launchers, config, or before a deploy-affecting change.     |
| `npm run preview`                             | Serves the built `dist/` for smoke testing.                                                                                                                                                                          | Manual browser/smoke verification after a build.                                                |
| `npm run ship:verify`                         | Read-only: polls the public build stamp (`/access-practice-lab/config.json`) until production serves the expected commit (default `origin/main`).                                                                     | After any deploy, or when checking whether production is fresh vs frozen.                       |
| `npm run audit`                               | `audit-curriculum.mjs` site-wide structural audit (links/redirects/orphans).                                                                                                                                         | Curriculum/lesson data changes.                                                                 |
| `npm run generate-curriculum-manifest`        | Rebuilds `data/curriculum-manifest.json` (curriculum SoT) from lesson configs + disk checks.                                                                                                                         | After adding/removing a lesson or its resources.                                                |
| `npm run audit:curriculum`                    | `audit-curriculum-resources.mjs` per-lesson resource-completeness audit → `reports/curriculum-audit-resources.{json,md}`.                                                                                            | Checking which lessons are missing family/teacher/student/etc. resources.                       |
| `npm run curriculum:scope`                    | Regenerates `docs/standards/scope-and-sequence.md` (Unit→Lesson→Standard→Title view) from lesson configs AND validates the spine (unknown standards, duplicate `unit·lesson` slots, numbering gaps). `--check` validates without writing (CI gate); exits 1 on any error. | Any scope/sequence or standards change; quick consistency check.                                |
| `npm run validate:scope`                      | The spine doctor in `--check` mode (`curriculum-scope-sequence.mjs --check`). **Part of `validate`**, so every `validate`/`ship` fails if a lesson references an unknown standard or two lessons collide on a `unit·lesson` slot.                                       | Runs automatically on every `validate`.                                                         |
| `npm run curriculum:rebuild`                  | **Seamless one-command adapt:** regenerates curriculum manifest + search index + launch manifest + scope-and-sequence view, then runs `validate:ccss` + `audit:curriculum`. Green = curriculum is internally consistent. | After editing any lesson `config.json`, `data/ccss-standards.json`, or applying a standards crosswalk. See [`docs/standards/msde-standards-change-runbook.md`](docs/standards/msde-standards-change-runbook.md). |
| `npm run generate-support-pages`              | Generates missing family/teacher-notes/student-help pages from lesson configs (skips `<!-- hand-edited -->`).                                                                                                        | Repairing missing lesson support pages.                                                         |
| `npm run validate:gate-coverage`               | **A validator that does not run is not a gate.** Enumerates every validator-shaped file under `tools/` and `scripts/` and fails unless each is either executed by the full gate or listed in `qa-exempt.json` with a written reason. Reachability is resolved by asking `scripts/qa-run.mjs --list` at runtime and walking `npm run` chains through package.json — it holds no copy of the gate, because a copy can drift from the gate and then report on a set nobody runs. Exemptions are held to the same standard as the tree: an entry whose file is deleted, or whose file the gate has since started running, FAILS and must be removed, and reasons under 25 characters are rejected. Written after an audit found **26 validator and audit files that `qa:loop` never executed**, nine of them with no npm script at all — including `smoke-injection`, `validate-printables-fresh` and `validate-scorm-self-contained`. None were broken; each was written for a real bug and then quietly never wired. Wiring those 26 fixes 26; this exists for the 27th. Self-tests seven detectors first. Part of `validate`. | Any new `validate-*`/`audit-*`/`smoke-*` tool; any change to the gate set. |
| `npm run smoke:injection`                     | Boots six representative pages in a real browser and fails on any page error, non-`/api` 4xx, or raw JS leaked into body text (the original injection symptom). **It served the SOURCE tree until 2026-08-20**, so every run failed 6/6 on `Failed to resolve module specifier "web-vitals"` and `"@engine/…"` — a bare npm specifier and a Vite alias, both resolved at BUILD time and impossible in source. It was reporting the build system working as designed as a product defect, which is why it was never wired and protected nothing. It now serves `dist/`, which is what Cloudflare serves, and ignores two harness-only noise classes: `/api/*` (no Worker in a static file server; that contract is held by `validate:auth-contract` and `validate:data-contracts`) and `ERR_ABORTED` for a file that EXISTS on disk (teardown, not a missing asset — an abort for a genuinely absent file still fails). Member of the `qa:loop` GATE, and EXCLUSIVE with `validate:lesson-boot` so two Chromiums never race. | Any injector change, engine/renderer change, or bulk edit of injected pages. |
| `npm run validate:printables-fresh`           | Asserts all 84 generated printables still match their sources — the staleness class where a regenerated lesson leaves the printed packet describing the old one. Fast (~3s). Part of `validate`. | Any change to a lesson config or a printable generator. |
| `npm run validate:interactive-upgrades`       | Structural check over the interactive lesson upgrade layer. Instant. Part of `validate`. | Any change to the interactive upgrade tooling. |
| `npm run validate:pdf-tagged`                 | Asserts every generated notes PDF (672 of them) is tagged, marked, and language-stamped — the accessibility properties a screen reader needs and that no visual check can see. Part of `validate`. | Any change to a PDF generator. |
| `npm run validate:course`                     | Asserts every Canvas quiz answer key (multiple-choice and matching) matches its source. Instant. Part of `validate`. | Canvas course/quiz export changes. |
| `npm run audit:games2d-esol`                  | Sweeps the 2D games for language-support regressions (589 files). Named `audit:` for historical reasons but it is pass/fail and fast, so it gates. Part of `validate`. | Any change to `scripts/games2d/` or game language content. |
| `node tools/audit-save-resume-integration.js` | Audits Save/Resume wiring across activities (same as `npm run validate:save-resume`; on failure it prints the `fix:save-resume` remediation command).                                                                | Any change near activity state, Save, or Resume.                                                |

> **Note:** `npm test` **does exist** — `node tools/run-tests.mjs`, which walks
> the repo for `*.test.{mjs,cjs,js}` (ignoring `node_modules`, `dist`, `.git`,
> `.qa-logs`, `coverage`) and runs 127 test scripts green in a few
> seconds (count as of 2026-08-05 — check `npm test` rather than trusting this
> number, which is the mistake this note exists to warn about; it had drifted
> from 108 to 127 before anyone re-read it). Several of those are RATCHETS rather than ordinary tests — they pin a
> number so a regression cannot be absorbed silently, and each one's failure
> message tells you exactly what to do:
> `tools/lint-coverage.test.mjs` (every shipped script must be visible to Biome),
> `tools/typecheck-ratchet.test.mjs` (the `@ts-nocheck` count may only shrink),
> `tools/curriculum-hub-assets.test.mjs` (the hub's extracted assets are stamped
> with their own content hash, so editing one without bumping `?v=` fails here
> instead of in a classroom — it checks **every tracked page** that references
> one, discovered by scanning rather than listed, because it used to read only
> `curriculum/index.html` and `curriculum/units/index.html` carried a
> `curriculum-hub-search.js` stamp matching no version of the file for this
> repo's whole history), `tools/headers-rules.test.mjs` (no two `_headers`
> rules that can match one URL may set the same header — Pages JOINS them),
> `tools/redirects-shadowing.test.mjs` (no `_redirects` rule may be unreachable),
> `tools/build-injectors-idempotent.test.mjs` (`npm run build` must not modify
> committed source), `tools/a11y-coverage.test.mjs` (the a11y sample must cover
> every page template), `functions/api-contract.test.mjs` (a new `/api`
> endpoint must use the shared handler in `functions/_lib/http.js`), and
> `tools/curriculum-json-cache.test.mjs` (every hub feature script that reads a
> shared `/data` manifest must go through `window.NTJsonCache`, and the hub must
> load it first — seven scripts each fetching the same two manifests is what put
> `/curriculum/` over its 60-request perf budget, and no per-file check can see
> it because each of those files is individually correct). It is wired into `npm run validate`, so every `ship` gates on it.
> `npm run check` (Biome: lint **+ formatting**) is a member of the `qa:loop`
> gate — it replaced `npm run lint` there on 2026-08-05, because `lint` alone
> says nothing about formatting, so the only thing checking it was the PR-only
> Pre-Deploy Gate. Deploys here go straight to `main` via `npm run ship`, which
> opens no PR, so 32 format errors banked up on `main` with every local gate
> green. `check` is a strict superset of `lint` and writes nothing, so the loop
> stays read-only. There is also `npm run e2e` (Playwright) and `npm run qa`
> (check + test + e2e).
>
> This note previously claimed the repo had no `npm test` and told agents not to
> invent one. That was wrong, and the cost was concrete: `assets/game-score.test.mjs`
> describes itself as guarding the counting contract that had already silently
> corrupted every accuracy figure on the site once — and because the doc said the
> runner did not exist, nobody checked `package.json`, so the test was never put
> in front of a deploy. **Verify a claim like this against `package.json` before
> repeating it.** If you add a new check, document it in this table.

If `node_modules` is missing, run `npm ci` first (or `npm install` if no
lockfile match), then run the checks.

---

## Hard Constraints

- **Deployment (as of 2026-06-08): push to `main` is the SINGLE deploy path.**
  The `neft-classroom-html-activities` Pages project (serving `eduwonderlab.com`)
  has Cloudflare Git integration **enabled** — production branch `main`, preview
  branches disabled. A push to `main` auto-runs `npm run build` (Vite) and
  promotes to production in ~1-2 min.
  - **Canonical deploy command: `ALLOW_DEPLOY=1 npm run ship -- <sha> [sha...]`**
    (`scripts/ship.sh`). It automates the entire known-good flow: stale-clone
    guard → fetch → clean detached worktree at `origin/main` → cherry-pick the
    named commits → rewrite private author emails (GH007) → push `main` (the
    pre-push QA loop gates it) → poll the public build stamp
    (`/access-practice-lab/config.json`) until production serves the new commit.
    `npm run ship:verify` is the read-only freshness check;
    `ALLOW_DEPLOY=1 npm run ship:rebuild` pushes an empty commit to unfreeze a
    stuck Pages build. Never assemble `main` by hand-pushing the working branch —
    the repo auto-commits during sessions, so `main` takes cherry-picked SHAs only.
    Full runbook + troubleshooting: [`docs/deploy.md`](docs/deploy.md).
  - **Do NOT run `wrangler pages deploy` manually.** Mixing manual wrangler with
    Git auto-deploy is what historically caused the site to "revert to an old
    version" (competing builds racing to production). One path only: `git push`.
  - Because CF rebuilds from committed source, **any change must be committed and
    pushed to `main` to go live** — an uncommitted or feature-branch change will
    not deploy, and will be reverted on the next `main` rebuild.
  - To add content: drop a self-contained top-level folder (`my-thing/index.html`
    - assets), commit, push. `vite.config.js` `copyStandaloneHtml()` copies every
      top-level dir into `dist/` except reserved names (engine, lessons, scripts,
      docs, node_modules, dist, dot-dirs); `.md` files are stripped.
- **Do NOT change the site structure or routes.** Routes are defined by
  `data/routes.json` → generated `_redirects`/`_headers`. Folder layout and URL
  paths are load-bearing (bookmarks, student links, save/resume keys). Edit
  content in place; do not move/rename/delete folders or restructure routes
  unless the task explicitly requires it.
- Do **not** push to GitHub or open a PR unless explicitly asked.
- Do **not** make broad, unrelated changes.
- Do **not** bypass permissions.
- Keep the repo a static-first, Cloudflare Pages-compatible, classroom-safe site.

See [`docs/closed-loop-qa-checklist.md`](docs/closed-loop-qa-checklist.md) for the
fill-in checklist to run before every handoff.

---

## Which checks actually block deployment?

Authoritative path (proven by `tools/deploy-path.test.mjs`):

```
ALLOW_DEPLOY=1 npm run ship -- <sha>
  → scripts/ship.sh cherry-picks onto origin/main
  → git push
  → .githooks/pre-push runs `npm run qa:loop`
  → scripts/qa-run.mjs GATE = build, check, validate (expanded),
    validate:homework, validate:practice, validate:lesson-boot,
    audit, audit:curriculum, audit:homework, smoke:injection
```

Every check is killed and FAILED after `QA_TIMEOUT_MS` (default 15 min). Until
2026-08-20 `execFile` was called with no timeout at all, so a hung check hung
`git push` with it, indefinitely and silently — whose realistic outcome is not a
patient wait but `--no-verify` becoming habit, disabling the whole gate. A
TIMEOUT is a failure, never a skip.

**Which validators are allowed NOT to gate is now itself gated.** Every
validator-shaped file under `tools/` and `scripts/` must be either reachable
from the gate or listed in [`qa-exempt.json`](qa-exempt.json) with a written
reason; `validate:gate-coverage` fails on any third case. See its row below.

**And the gates are proven able to fail.** `tools/gate-mutation.test.mjs` (in
`npm test`, so on every push) plants a defect each gate SHOULD catch and asserts
it exits non-zero — a gate that has quietly stopped firing and a gate watching a
clean tree otherwise print the same line. Mutations are **additive only** (a new
file, deleted after; no tracked file is ever modified, which is why the
content-pinned `validate:auth-contract` is deliberately not a subject), and each
runs against a **throwaway copy of the git index** via `GIT_INDEX_FILE` — these
validators enumerate with `git ls-files`, so an untracked mutation would be
invisible and prove nothing, and staging it for real would mutate the index
during a push. `QA_MUTATE_SLOW=1 npm test` adds the ~30s `validate:js-syntax`
case.

`npm run validate:production` is a **readiness report**, not the push gate.
It is honest about skips (`SKIPPED` / `NOT AVAILABLE` → exit 2). Local
`validate:lesson-boot` without Chromium still exits 0 inside `qa:loop` so
everyday pushes are not blocked on a missing browser.

Not deploy gates (diagnostic / optional / manual):

- `diagnose:student-access` — classroom-network Access vs Pages
- `diagnose:production-access` — student SCORM runtime vs teacher protection on live origin
- `smoke:live` — post-deploy / Site Health; Access intercept is NOT AVAILABLE
- `audit:a11y`, `audit:dead-code`, `audit:duplicates`, `audit:deps` — reports
- `sweep:small-group`, `e2e:auth`, `e2e:supports` — need a server; run before shipping those areas
- `backup:d1`, `report:usage` — operational, not a source gate

---

## Automated QA Loop

A repeatable, conservative build → audit → fix → retest loop, defined by
[`scripts/qa-run.mjs`](scripts/qa-run.mjs) (parallel scheduler, the default) and
[`scripts/qa-loop.sh`](scripts/qa-loop.sh) (the serial original, kept as
`npm run qa:loop:serial` and read by `tools/qa-run.test.mjs` as the coverage
ratchet).

- **How to run:** `npm run qa:loop`. The serial script detects which check
  scripts actually exist, runs the safe ones (`build`, `check`, `test`,
  `validate*`, `audit*`), prints PASS / SKIP / FAIL per check, and writes a
  timestamped log to `.qa-logs/`. It exits non-zero if any available check
  fails. `npm run qa:danger -- "<cmd>"` checks whether a command is one of the
  blocked dangerous ones (`scripts/check-dangerous-commands.sh`; exit 2 =
  blocked). The slash commands that exist are `/qa-check`, `/new-activity` and
  `/ship` — see `.claude/commands/`.
- **What it checks:** Vite build + the repo's `validate`/`audit` suites. It never
  runs generators or `deploy`.
- **What it refuses to do:** deploy, commit, push, force-push, delete/move lesson
  folders or routes, restructure curriculum/pages, or replace working content
  with placeholders. These are enforced by `permissions.deny` in
  `.claude/settings.json` — 11 rules covering `wrangler deploy`, `npm run
  deploy`, force-push, `git reset --hard`, `git clean -fd` and `rm -rf` — and,
  since 2026-08-06, by the `pre-bash-guard.sh` PreToolUse/Bash hook, which is
  now written, wired and verified running (see the note under **Stop rule**).
  `npm run qa:danger` runs the same checks on a command by hand.
- **Deploy rule:** push to `main` is the only deploy path (Cloudflare Git
  integration), and `ALLOW_DEPLOY=1 npm run ship -- <sha>` is the only supported
  way to push it. **Never** run `wrangler` / `npm run deploy` manually — it is
  blocked.
- **Structure rule:** do not change curriculum / page / route / lesson-card
  structure unless Joel explicitly asks.
- **Stop rule:** stop after available checks pass (twice in a row), or when the
  same failure repeats and needs human judgment, or when the only fix left would
  be a risky structural change — then produce the final report described in
  [`docs/closed-loop-qa-checklist.md`](docs/closed-loop-qa-checklist.md).

> **Hooks — what is actually wired.** `.claude/settings.json` declares two:
> `SessionStart → .claude/hooks/session-start.sh` and `PreToolUse(Bash) →
> .claude/hooks/pre-bash-guard.sh`. Both scripts exist and both were verified by
> execution, not by reading the config.
>
> History worth keeping: the config once declared three hooks whose scripts did
> not exist. A hook pointing at a missing script does not announce itself — it
> simply never runs — so the repo documented three safety behaviours that were
> not happening, and the dead declarations were removed on 2026-08-05. The Bash
> guard was then written for real and wired on 2026-08-06, after
> `git update-ref -d refs/heads/<x>` was found to delete a branch straight past
> the `git branch -D` entry in `permissions.deny`. **Verify a hook by running
> it** (`echo '{"tool_input":{"command":"…"}}' | bash .claude/hooks/pre-bash-guard.sh`;
> exit 2 means blocked), never by trusting this file.
>
> The guard delegates to `scripts/check-dangerous-commands.sh`, which is tracked
> and covered by `scripts/check-dangerous-commands.test.mjs` — that test pins
> both halves, what must block AND what must stay allowed, because the first
> attempt at the ref-deletion rules also blocked the safe `git branch -d`.
>
> **Resolved 2026-08-09.** This used to read: "`.claude/` is excluded via
> `.git/info/exclude`, so the hook scripts and `settings.json` hook block are
> machine-local — a fresh clone gets the blocklist script and its test but not
> the wiring. Re-add the `PreToolUse` block after cloning."
>
> Half of that had stopped being true, in the worse direction. `.gitignore`
> un-ignores `.claude/settings.json` and `.claude/hooks/`, so the **wiring** was
> tracked while `pre-bash-guard.sh` was **not** — a fresh clone got a
> `PreToolUse` hook pointing at a missing script. That is exactly the failure
> described two paragraphs up, and this file asserted "both scripts exist" while
> it was happening. Reading could never have caught it: a hook that never runs
> looks identical to a hook that allows everything.
>
> `pre-bash-guard.sh` is now committed, so the wiring resolves on a fresh clone.
>
> **`npm run validate:graph` now gates this.** For eight days this paragraph told
> you to run a script that did not exist; the correction that replaced it then
> went too far the other way and said the check "was never written". Both were
> wrong. `tools/graph/validate-graph.mjs` HAS been in the repo the whole time and
> does exactly the documented job — plus two more invariants — it simply had no
> npm script, so nothing ever ran it:
>
>   1. every artifact in `tools/graph/deploy-graph.json` exists at the path claimed;
>   2. every mirror names the artifact it mirrors AND a gate (an ungated mirror is
>      how fix-it-design-challenge drifted 3,800 lines behind the page it mirrored);
>   3. **every hook command in `.claude/settings.json` points at a file that
>      exists** — the check that would have caught `pre-bash-guard.sh` being wired
>      into `PreToolUse` while absent from the repo.
>
> Wired 2026-08-14 into `validate` and into `qa:fast` coverage for
> `tools/graph/**` and `.claude/{settings.json,hooks/}`. Cross-repo paths are
> reported as un-checkable rather than passed silently. Verify a hook by RUNNING
> it as well — the gate proves the file exists, not that it behaves.
